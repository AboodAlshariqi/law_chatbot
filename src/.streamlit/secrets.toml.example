OPENROUTER_API_KEY = "sk-or-v1-your_key_here"
# Optional — only needed if you switch the LLM provider to Groq (faster, but a stricter free-tier
# token limit) in the app's Settings panel. Chat works fine with just OPENROUTER_API_KEY above.
GROQ_API_KEY = "gsk_your_key_here"
